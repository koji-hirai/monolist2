<div class="side-box">
  <h4 class="side-box-title"><%= action %>したユーザー</h4>
  <div class="side-box-body">
    <% if ownership_users.any? %>
      <div class="user_avatars">
        <% ownership_users.each do |ownership_user| %>
        <div>
          <span><%= link_to gravatar_for(ownership_user, size: 30), ownership_user %></span>
          <p class="arrow_box"><%= ownership_user.name %></p>
        </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
```
